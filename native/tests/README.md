# Native Tests

This directory contains native test infrastructure.

Current coverage is compile-only. It verifies that public SCL headers are usable from a standalone C++20 target.

Protocol behavior tests begin with future implementation RFCs.
